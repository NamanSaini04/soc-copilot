# Endpoint Investigation Playbook
## Investigation
Collect process tree, command line, parent process, signed-binary status, file hashes, network connections, user context, persistence changes, and EDR detections. Encoded PowerShell is suspicious but can have legitimate administrative uses.
## Containment
After analyst authorization, isolate a host when impact and evidence justify it. Preserve volatile evidence first where feasible and document every action.

